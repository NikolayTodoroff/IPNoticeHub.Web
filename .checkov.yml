framework:
  - terraform

skip-check:
  - CKV_AZURE_63   # HTTP logging adds storage costs, Application Insights handles monitoring
  - CKV_AZURE_17   # Client certificates not needed — app uses standard authentication
  - CKV_AZURE_88   # Azure Files mount not required for this deployment
  - CKV_AZURE_225  # Zone redundancy requires Premium App Service Plan
  - CKV_AZURE_212  # Single instance sufficient for demo — failover scaling not required
  - CKV_AZURE_213  # Health check endpoint not implemented in application code
  - CKV_AZURE_42   # Soft delete and purge protection add complexity for demo — vault is easily recreated
  - CKV_AZURE_109  # Firewall rules would block GitHub-hosted runners with dynamic IPs
  - CKV_AZURE_66   # Failed request tracing not needed — Application Insights handles monitoring
  - CKV_AZURE_65   # Detailed error messages expose internals, disabled intentionally for security
  - CKV_AZURE_222  # Public access needed — no VNet integration for demo deployment
  - CKV_AZURE_13   # App Service Authentication (EasyAuth) not used — app handles its own auth

  - CKV_AZURE_229  # Zone redundancy requires Premium SKU, not needed for this deployment
  - CKV_AZURE_224  # Ledger feature is for audit/compliance scenarios, not needed for demo
  - CKV_AZURE_52   # TLS version controlled by Azure default, explicit setting not required
  - CKV_AZURE_113  # Public access needed — app connects over public endpoint
  - CKV_AZURE_41   # Secret expiration not set — demo secrets are static, rotated manually
  - CKV_AZURE_114  # Content type on secrets is informational metadata, not a security risk
  - CKV_AZURE_110  # Purge protection adds complexity for demo — vault is easily recreated
  - CKV_AZURE_189  # Public access needed — GitHub-hosted runners access over public internet
  - CKV2_AZURE_32  # Private endpoint requires VNet integration, not justified for demo

  - CKV_AZURE_23   # SQL auditing adds storage costs, not needed for demo database
  - CKV_AZURE_24   # Auditing retention requires auditing to be enabled first
  - CKV2_AZURE_27  # Azure AD auth for SQL not configured — app uses SQL authentication
  - CKV2_AZURE_34  # Firewall rules allow app service and local IP access intentionally
  - CKV2_AZURE_45  # Private endpoint requires VNet integration, not justified for demo
  - CKV2_AZURE_2   # Vulnerability Assessment requires storage account and additional config